#ifndef PRIME_HPP //ueberprueft, ob prime.hpp schon definiert wurde
#define PRIME_HPP 1 //definiert prime.hpp, falls dies nicht schon geschehen ist

/* externe Funktionen und Variablen */
extern bool isPrime (unsigned int zahl);
extern int nPrime (int n);

#endif //falls prime.hpp schon definiert wurde wird der Inhalt der if-Bedingung uebersprungen
